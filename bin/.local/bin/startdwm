#!/bin/sh

while true; do
	# restart dwm without logging out or closing appications : Mod-Shift-Q
    # Log stderror to a file 
    dwm 2> ~/.dwm.log
    # No error logging
    #dwm >/dev/null 2>&1
    [ -f ~/.profile ] && source ~/.profile
done
