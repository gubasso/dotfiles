#!/usr/bin/bash
until busctl --user --quiet get-property \
    org.kde.StatusNotifierWatcher /StatusNotifierWatcher \
    org.kde.StatusNotifierWatcher IsStatusNotifierHostRegistered >/dev/null 2>&1; do
        sleep 0.2
done

