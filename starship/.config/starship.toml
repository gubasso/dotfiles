format = """
$username\
$hostname\
$container\
${env_var.DEV_SANDBOX}\
$directory\
$git_branch\
$git_state\
$git_status\
$cmd_duration\
$python\
$aws\
$gcloud\
$azure\
$line_break\
$character"""

[directory]
read_only = " 󰌾"
style = "blue"

[character]
success_symbol = "[❯](bright-purple)"
error_symbol = "[❯](red)"
vimcmd_symbol = "[❮](green)"

[container]
symbol = "🧊"
style = "bright-black"
format = "[$symbol $name]($style) "

[custom.container_engine]
# Keep 'when' cheap to avoid timeouts; rely on common indicators.
# If you prefer maximal coverage, see the "Accurate gating" variant below.
when = '''
test -n "${container-}" \
  || test -r /run/host/container-manager \
  || test -f /.dockerenv \
  || test -f /run/.containerenv
'''

command = '''
t=""

# 1) Prefer $container if present
if [ -n "${container-}" ]; then
  t="$container"

# 2) Toolbox/Flatpak-style hint file, if present
elif [ -r /run/host/container-manager ]; then
  IFS= read -r t < /run/host/container-manager || true
fi

# 3) Fallback to systemd-detect-virt only if still unknown
if [ -z "$t" ]; then
  t="$(systemd-detect-virt --container 2>/dev/null || true)"
fi

# Hide module if unknown / none
[ -z "$t" ] && exit 1
[ "$t" = "none" ] && exit 1

case "$t" in
  docker)          echo " docker" ;;
  podman)          echo "󰡄 podman" ;;
  systemd-nspawn)  echo "󱄚 nspawn" ;;
  lxc|lxc-libvirt) echo "󱄚 $t" ;;
  openvz)          echo "󱄚 openvz" ;;
  rkt)             echo "󰡄 rkt" ;;
  wsl)             echo " wsl" ;;
  proot)           echo "󰏗 proot" ;;
  pouch)           echo "󰏗 pouch" ;;
  *)               echo "󰏗 $t" ;;
esac
'''

style  = "bright-black"
format = "[$output]($style) "

[env_var.DEV_SANDBOX]
style = "bright-black"
format = "[$env_value]($style) "

[git_branch]
symbol = " "
format = "[$branch]($style)"
style = "bright-black"

[git_status]
format = "[[(*$conflicted$untracked$modified$staged$renamed$deleted)](218) ($ahead_behind$stashed)]($style)"
style = "cyan"
conflicted = "​"
untracked = "​"
modified = "​"
staged = "​"
renamed = "​"
deleted = "​"
stashed = "≡"

[git_state]
format = '\([$state( $progress_current/$progress_total)]($style)\) '
style = "bright-black"

[git_commit]
tag_symbol = '  '

[cmd_duration]
format = "[$duration]($style) "
style = "yellow"

[python]
symbol = " "
format = "[$virtualenv]($style) "
style = "bright-black"

[aws]
symbol = ' '
format = '[$symbol]($style)'

[azure]
disabled = false
format = '[$symbol]($style)'
symbol = "󰠅 "

[gcloud]
disabled = true
symbol = " "
format = '[$symbol]($style)'

[custom.gcloud]
command = 'echo " "'
when    = 'gcloud auth print-access-token --quiet >/dev/null 2>&1'
style   = 'bold blue'
format  = '[$output]($style)'

[docker_context]
symbol = " "

[golang]
symbol = " "

[hostname]
ssh_symbol = " "

[lua]
symbol = " "

[memory_usage]
symbol = "󰍛 "

[nodejs]
symbol = " "

[os.symbols]
Alpaquita = " "
Alpine = " "
AlmaLinux = " "
Amazon = " "
Android = " "
Arch = " "
Artix = " "
CachyOS = " "
CentOS = " "
Debian = " "
DragonFly = " "
Emscripten = " "
EndeavourOS = " "
Fedora = " "
FreeBSD = " "
Garuda = "󰛓 "
Gentoo = " "
HardenedBSD = "󰞌 "
Illumos = "󰈸 "
Kali = " "
Linux = " "
Mabox = " "
Macos = " "
Manjaro = " "
Mariner = " "
MidnightBSD = " "
Mint = " "
NetBSD = " "
NixOS = " "
Nobara = " "
OpenBSD = "󰈺 "
openSUSE = " "
OracleLinux = "󰌷 "
Pop = " "
Raspbian = " "
Redhat = " "
RedHatEnterprise = " "
RockyLinux = " "
Redox = "󰀘 "
Solus = "󰠳 "
SUSE = " "
Ubuntu = " "
Unknown = " "
Void = " "
Windows = "󰍲 "

[package]
symbol = "󰏗 "

[rlang]
symbol = "󰟔 "

[ruby]
symbol = " "

[rust]
symbol = "󱘗 "

[zig]
symbol = " "
